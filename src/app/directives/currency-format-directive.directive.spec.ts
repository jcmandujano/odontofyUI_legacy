import { CurrencyFormatDirectiveDirective } from './currency-format-directive.directive';

describe('CurrencyFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
